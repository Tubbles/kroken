# 2026-09-06

Tags: #decision #editor #micro

## micro integration lands as a bundled plugin with buffer anchors

- **Where**: the plugin lives in the Tubbles/micro fork as `runtime/plugins/kroken/` on branch `kroken`, not in this repository, as PLAN.md milestone 2 intended. Bundled means a `make build` of the fork carries it, with no plugin manager step.
- **Paste-back strategy**: editor marks (strategy 1 in `doc/editor-integration.md`), implemented as buffer anchors added to the fork on top of its existing text-edit listener hook. Text search was rejected: micro's `FindNext` is line-based, so it can never find a multi-line selection, and it picks the wrong occurrence when the selection text is duplicated. The anchor's text is compared with the sent selection before pasting, so edits inside the region cancel the paste instead of clobbering them.
- **Three corrections to the old micro sketch**, now recorded in `doc/editor-integration.md`: stdout and stderr arrive mixed in the exit callback, the exit status must be read from the job's `ProcessState`, and `FindNext` cannot match across lines.
- **No kroken changes were needed.** The protocol held up as designed: `--selection-file` for the stdin micro cannot close, 1-based inclusive positions, stdout verbatim, status on stderr, exit codes.

## Installation and releases

Tags: #decision #release #install

- `scripts/install.sh` copies the binary to `<prefix>/bin`; a symlink stays the documented option for development. No package manager format yet.
- Releases are cut by pushing a `vX.Y.Z` tag. The workflow rebuilds from scratch and fails when the binary's reported version differs from the tag, so a forgotten `VERSION` bump cannot ship. CI artifacts expire after 90 days, which is why a release is the distribution channel for machines without the toolchain.

## Configuration format moves from TOML to SJSON

Tags: #decision #config #sjson #odin

- **Why**: the owner's global preference is SJSON (Bitsquid simplified JSON) wherever the language supports it natively, and Odin does through `core:encoding/json` with `Specification.SJSON`. The hand-written TOML package (about 900 lines with tests) is deleted in favour of that parser.
- **Breaking**: `config.toml` becomes `config.sjson`, `config.d/*.toml` becomes `config.d/*.sjson`, `.kroken` keeps its name but changes syntax. `[profile.x]` tables become members of a `profiles` object. Dotted keys are gone; nest with braces. Version bumped to 0.2.0.
- **Multi-line text**: SJSON strings cannot span lines, so `prompt.system` and `prompt.template` accept an array of lines (joined with newlines, trailing newline added). `kroken config` prints them back in that form.
- **Guards around the core parser**: an empty or comment-only file is treated as an empty object (the core parser reports `Unexpected_Token` at EOF), and a pre-scan rejects unterminated strings because the core parser silently yields `""` for them (see SUGGESTIONS.md for the upstream note). Positions from the core tokenizer are unreliable for duplicate keys (it reports the inserted comma's position), so the test only checks the message there.
- **Dump**: `json.marshal` of a mirror struct in MJSON mode with `=` and pretty printing; a registered user marshaler prints floats in shortest form instead of the sixteen decimals the default emits.

## Backends: Claude Code and OpenAI Codex as equals

Tags: #decision #backend #codex #openai #architecture

- **Shape**: a `backend.Backend` value (name, description, `build`, `parse`, `set_model`) per agent CLI, in its own package under `src/backend/`. The generic package runs the process and owns the run directory; the CLI holds the list. Nothing outside a backend's package refers to it by name.
- **OpenAI means Codex CLI**: `codex exec` is the `claude -p` counterpart (headless, agentic, reads AGENTS.md, ChatGPT login or API key). Calling the OpenAI HTTP API directly was rejected: Odin's core library has no TLS, and a plain chat completion would lose the repository-reading tools the whole workflow relies on.
- **Codex specifics**: the output schema must be a file (`--output-schema <path>`), so every run now gets a directory up front, temporary when logging is off. Codex exec has no system prompt flag, so the backend prepends the system prompt to the stdin text. `-c model_reasoning_effort="..."` carries the effort; `--ephemeral` and `--skip-git-repo-check` are on by default. Results come from the JSON Lines stream: the last `agent_message` item is the schema-shaped answer, `turn.completed` carries usage, `turn.failed` and `error` carry failures.
- **Untested live**: no OpenAI account is available here. The Codex backend is tested against the event stream documented by OpenAI and the flags from its CLI reference; the first live run must confirm that `codex exec -` accepts the prompt on stdin together with `--output-schema`.
- **Run files renamed**: `result.json` became `stdout.txt`, since Codex's stdout is JSON Lines rather than one object.

## One documentation source for help, man page, and repository

Tags: #decision #docs #manpage

- **Single source**: `doc/configuration.md` and `doc/editor-integration.md` (plus `README.md`) are the reference. The binary embeds them with Odin's `#load` for `kroken help <topic>`, so the text always matches the build and is available where only a release binary exists. The man page is assembled from the same files.
- **Man page tooling**: go-md2man, because it installs without root (`go install`), converts the existing Markdown including tables, and the CI runner has Go. pandoc was the alternative and would work on the same input except for its title-line convention; it is not wired up. scdoc and mdoc were rejected because they need a second source format.
- **Man-only text**: `doc/man/kroken.md` holds NAME, SYNOPSIS, COMMANDS, EXIT STATUS, ENVIRONMENT, FILES. The flag tables inside it come from the binary's own `--help` output at build time, captured with `HOME=/home/user` so no machine path lands in an installed page.
- **Installation**: `scripts/install.sh` puts `kroken.1` under `<prefix>/share/man/man1`; man-db finds `~/.local/share/man` on its own once `~/.local/bin` is on PATH. Releases attach `kroken.1` as an asset for machines that only download the binary.
